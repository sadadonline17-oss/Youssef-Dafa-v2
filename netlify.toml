[build]
  command = "npm ci --force && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--force"
  NETLIFY_USE_X64 = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
